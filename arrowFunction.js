function soma(x, y) {
    return x + y;
}

const multiplicacao = function(x, y) {
    return x * y;
};

const dividir = (x, y) => {
    return x / y;
};

const objeto = () => ({nome: 'Engelbert', sobrenome: 'Paiva'});

console.log(soma(3, 9));

console.log(multiplicacao(5, 9));

console.log(dividir(25, 5));

console.log(objeto);